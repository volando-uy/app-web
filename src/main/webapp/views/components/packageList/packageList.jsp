<section class="w-full flex flex-col items-center justify-center py-12">
  <div class="w-full max-w-4xl flex flex-col items-center justify-center">
    <div id="package-carousel" class="relative w-full h-[420px] flex items-center justify-center overflow-hidden rounded-3xl shadow-2xl">
      <!-- Carousel se inyecta por JS -->
    </div>
  </div>
</section>

<!-- Raíz de modales (se inyecta por JS si no existe, esto es por si preferís dejarlo explícito) -->
<div id="modal-root"></div>